<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spd.system.mapper.SysPostMenuMapper">

	<resultMap type="SysPostMenu" id="SysPostMenuResult">
		<result property="menuId"     column="menu_id"      />
		<result property="postId"     column="post_id"      />
	</resultMap>

	<delete id="deletePostMenuByPostId" parameterType="Long">
		delete from sys_post_menu where post_id=#{postId}
	</delete>

	<insert id="batchPostMenu">
		insert into sys_post_menu(post_id, menu_id) values
		<foreach item="item" index="index" collection="list" separator=",">
			(#{item.postId},#{item.menuId})
		</foreach>
	</insert>

	<select id="selectMenuListByPostId" parameterType="Long" resultType="java.lang.Long">
		select menu_id from sys_post_menu where post_id=#{postId}
	</select>

</mapper>

