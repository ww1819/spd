# 总览 — 共性需求与问题

> **用途**：记录**整体 / 多租户共用**的需求、缺陷与改造。  
> Agent 读到本文档时：修改**共享**前后端逻辑或共享页面，对全部租户（或条目中写明的多个租户）生效。  
> **仅某一医院**的需求请写到对应租户目录的 `需求与问题.md`，不要写在这里。

关联：
- 租户入口与差异对照：[README.md](./README.md)
- 隔离与拆页规范：[ISOLATION.md](./ISOLATION.md)

## 租户 ID 对照（分支时禁止混用）

| 医院 | tenant_key | customerId |
|------|------------|------------|
| 衡水市第三人民医院 | `HS_003` | `hengsui-third-001` |
| 枣强县中医院 | `ZQ_TCM` | `zaoqiang-tcm-001` |
| 默认租户 | `DEFAULT` | `tenant-default` |
| 示例 A | `TENANT_A` | `tenant-a-001` |
| 示例 B | `TENANT_B` | `tenant-b-002` |

---

## 条目模板

```markdown
### YYYY-MM-DD — 简短标题
- 类型：需求 | 问题/缺陷 | 技术改造
- 影响范围：全部租户 | 列出枚举名（如 HS_003 + ZQ_TCM）
- 菜单/模块：
- 现状：
- 期望：
- 验收要点：
- 状态：待做 | 进行中 | 已完成
- 完成说明：（完成后由 Agent 或开发补一句）
```

---

## 共性需求与问题列表

### 2026-08-21 — 耗材名称模糊检索中文与拼音条数不一致
- 类型：问题/缺陷
- 影响范围：全部租户
- 菜单/模块：基础资料 / 耗材产品维护
- 现状：输入拼音简码如 `fgf` 与中文「发光法」条数不一致（例如 56 vs 51）
- 期望：中文关键字按名称匹配，并与其拼音简码 OR；纯字母只按 `referred_name` 简码匹配，不再与名称条件 AND 收窄
- 验收要点：`发光法` 与 `fgf` 结果集接近（简码含 FGF 或名称含发光法均可命中）；纯字母不再误走名称字段 AND
- 状态：已完成
- 完成说明：`FdMaterialMapper.xml` 名称条件改为 OR；`material/index.vue` 字母检索只传 `nameSearch`

### 2026-08-21 — 耗材产品维护改为公共菜单（含枣强）
- 类型：需求
- 影响范围：全部启用耗材客户（含 HS_003、ZQ_TCM）
- 菜单/模块：基础资料 / 耗材产品维护（`foundation/material/index`）
- 现状：页面为共享页且已有枣强分支，但客户菜单可能未对枣强开通，导致侧栏看不到
- 期望：菜单 `default_open_to_customer=1`，并回填 `hc_customer_menu` / 已有基础资料用户的 `sys_user_menu` / super 岗位菜单
- 验收要点：枣强切换组织后可见「耗材产品维护」；仍保持枣强禁手工新增、编辑限字段；衡水行为不变
- 状态：已完成
- 完成说明：`column.sql` 回填公共授权；页面逻辑仍按 `isZqTcmTenant` / `isHsThirdTenant` 分支，未把单院特例写成默认

### 2026-08-12 — 出/退库汇总表增加「计费」列
- 类型：需求
- 影响范围：全部租户
- 菜单/模块：库房管理 / 出/退库查询 / 出/退库汇总表
- 现状：汇总表在「储存方式」后无「计费」列；明细表已有该列
- 期望：在「储存方式」后增加「计费」列，显示「是/否」，取值耗材档案维护 `is_billing`（与明细表一致）；导出同步带上该列
- 验收要点：汇总列表与导出均在储存方式后展示计费；是=档案计费，否=不计费/其它；全租户生效（非单院分支）
- 状态：已完成
- 完成说明：汇总 SQL 回传 `materialIsBilling`；`secondOutQuery` 与导出 `exportCTKWarehouseSummaryListStyledXlsx` 已增加计费列

### 2026-08-22 — 页签切回后列表页顶部留白被挡
- 类型：问题/缺陷
- 影响范围：全部租户
- 菜单/模块：全局布局（页签切换 / keep-alive）；典型复现：基础资料 / 耗材产品维护 ↔ 到货验收
- 现状：从其他页签切回缓存列表页时，主内容区 scrollTop 残留，固定头下整页上移，搜索区顶部 8px 留白消失
- 期望：任意页签切回后与首次打开该页布局一致
- 验收要点：耗材产品维护 → 到货验收 → 页签切回，顶部留白与首次打开一致；筛选/勾选缓存仍保留；其他模块页签切换无回归
- 状态：已完成
- 完成说明：`spd-ui` 新增 `utils/reset-main-scroll.js`；`permission.js` `afterEach` 与 `AppMain` 路由切换/过渡完成后统一调度滚动复位

（在此下方追加）
