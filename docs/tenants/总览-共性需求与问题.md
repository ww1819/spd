# 总览 — 共性需求与问题

> **用途**：记录**整体 / 多租户共用**的需求、缺陷与改造。  
> Agent 读到本文档时：修改**共享**前后端逻辑或共享页面，对全部租户（或条目中写明的多个租户）生效。  
> **仅某一医院**的需求请写到对应租户目录的 `需求与问题.md`，不要写在这里。

关联：
- 租户入口与差异对照：[README.md](./README.md)
- 隔离与拆页规范：[ISOLATION.md](./ISOLATION.md)

## 租户 ID 对照（分支时禁止混用）

| 医院 | tenant_key | customerId |
|------|------------|------------|
| 衡水市第三人民医院 | `HS_003` | `hengsui-third-001` |
| 枣强县中医院 | `ZQ_TCM` | `zaoqiang-tcm-001` |
| 默认租户 | `DEFAULT` | `tenant-default` |
| 示例 A | `TENANT_A` | `tenant-a-001` |
| 示例 B | `TENANT_B` | `tenant-b-002` |

---

## 条目模板

```markdown
### YYYY-MM-DD — 简短标题
- 类型：需求 | 问题/缺陷 | 技术改造
- 影响范围：全部租户 | 列出枚举名（如 HS_003 + ZQ_TCM）
- 菜单/模块：
- 现状：
- 期望：
- 验收要点：
- 状态：待做 | 进行中 | 已完成
- 完成说明：（完成后由 Agent 或开发补一句）
```

---

## 共性需求与问题列表

### 2026-09-24 — 科室首页建设：顶部五指标
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：首页（homeView=department）
- 现状：科室首页为空占位
- 期望：参考默认首页布局；顶部五指标（科室待申领审批、科室申领在途物资、科室耗材近效期、科室库存余量、科室近期消耗条数）；下方沿用默认近七日领用趋势、库存结构占比、常用菜单
- 验收要点：授权科室首页后可见五卡与两图；点击可跳转对应业务菜单；近效期/库存余量进入科室库存查询对应 Tab
- 状态：已完成
- 完成说明：`departmentHomeStats`；复用待审申领/待收货/近效期计数；库存余量=科室库存 qty 合计；近期消耗=近7日已审消耗明细条数；图表复用默认 `kpiTrend.outCount` + `outboundFinanceCategoryProportion`

### 2026-09-24 — 库房首页建设：顶部五指标
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：首页（homeView=warehouse）
- 现状：库房首页为空占位
- 期望：参考默认首页布局；顶部五指标（待盘点任务数量、盘点差异条目数、移库待处理单据、退货待验收数量、库区超储物资数量）；下方沿用默认近七日领用趋势、库存结构占比、常用菜单
- 验收要点：授权库房首页后可见五卡与两图；点击可跳转对应业务菜单；超储卡进入库存查询预警 Tab
- 状态：已完成
- 完成说明：`warehouseHomeStats`；库房视图复用默认 `kpiTrend.outCount` + `outboundFinanceCategoryProportion`

### 2026-09-24 — 采购首页建设：顶部五指标
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：首页（homeView=purchase）
- 现状：采购首页为空占位
- 期望：参考默认首页布局；顶部五指标；左图近7日采购到货趋势（入库数量）；右图当月采购分类金额占比（财务分类入退货）；常用菜单
- 验收要点：授权采购首页后可见五卡与两图；左图按日入库波动；右图按财务分类金额占比；待比价暂 0
- 状态：已完成
- 完成说明：`purchaseHomeStats`；采购视图复用 `kpiTrend.inCount` + `inboundFinanceCategoryProportion`

### 2026-09-24 — 京东分栏二级目录合并脚本 + 更新自动补齐
- 类型：技术改造
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：采购/科室/库房/基础资料/系统管理 二级目录
- 现状：目录调整散落多个 `*_public.sql`；还原生产库后需手工逐个重跑；`menu.sql` 未含 3946–3958
- 期望：单独一份幂等脚本；系统更新重启时自动执行补齐
- 验收要点：`spd.sql.init.enabled=true` 重启后可见基础资料三分栏、采购三目录、科室领用/采购、库存管理、人员权限；重复启动不报错不重复脏数据
- 状态：已完成
- 完成说明：`sql/mysql/material/menu_dir_restructure.sql`；`SqlInitRunner` 在 `menu.sql` 之后执行；手工入口见 `sql/menu_dir_restructure_public.sql`

### 2026-09-24 — 默认首页近七日趋势/库存结构占比不显示
- 类型：问题/缺陷
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：首页（默认 homeView=simple）
- 现状：顶部 KPI 有数，近七日领用趋势与库存结构占比空白
- 期望：两图正常出数
- 验收要点：有出库业务时趋势非全 0；当月有出退库时饼图有分类；重启后端后生效
- 状态：已完成
- 完成说明：`kpiTrend` 读 HashMap 忽略大小写；趋势按 `coalesce(audit_date,bill_date)`；默认首页趋势改为出库数量+出库金额双折线（金额优先明细 `amt`）；图例居中避免挡轴；前端饼图字段兼容小写 + resize

### 2026-09-24 — 库存明细选「二级库（原有库存）」查不到
- 类型：问题说明（非缺陷）
- 影响范围：衡水三院 `hengsui-third-001`（同类二级库亦同）
- 菜单/模块：库房管理 → 库存查询 → 库存明细查询
- 现状：选「二级库（原有库存）」结果 0 条
- 原因：该仓 `stk_inventory` 122 行数量均为 0；有数量的库存在 `stk_dep_inventory`（科室侧）
- 期望/用法：库房页点「零库存」看零数量明细；有数量请到「科室库存查询」
- 状态：已说明；空表提示已补充

- 类型：问题/缺陷
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：科室管理 → 科室库存查询 → 库存明细查询
- 现状：列「出库日期」绑定 `materialDate`，实际为仓库入库时写入的耗材日期，与出库单号日期不一致
- 期望：显示对应出库单 `audit_date`（审核日期）
- 验收要点：有 `bill_id` 的行与出库审核日一致；历史无单据关联行仍可回退到表字段；新出库审核写入科室库存的 `material_date` 为审核日
- 状态：已完成
- 完成说明：列表按出库单号/`bill_id` 取审核日；`StkDepInventoryServiceImpl.fillOutboundAuditDates` 强制回填（避免仅改 XML 未进 classpath）；新出库写入审核日

### 2026-09-24 — 首页常用菜单支持手动设置
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：首页 → 常用菜单
- 现状：仅按点击频率自动展示；无法指定要显示的菜单
- 期望：标题栏右侧增加编辑图标；弹窗勾选要展示的常用菜单；仅列出当前账号有权限的菜单；无权限项不出现也不展示；两行×8列展示，超出定时左右轮播
- 验收要点：编辑图标可见；弹窗仅含侧栏有权菜单；保存后首页按选择展示；「恢复自动」后回到按频率；权限收回后对应项自动消失；超过 16 个自动轮播
- 状态：已完成
- 完成说明：`spd-ui/src/views/index.vue`；配置键 `spd.homeFrequentMenus`；上限 48；`HomeUserWorkspaceService.topMenus` 上限同步到 48

### 2026-09-24 — 工作组授权增加「首页设置」并可同步到组内用户
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：系统管理 → 工作组 → 授权 → 首页设置；同步首页设置
- 现状：工作组授权仅有消息提醒等，无首页设置；无法批量下发到组内账号
- 期望：消息提醒后增加「首页设置」（同用户授权：默认/完整/采购/库房/科室，「默认」点亮）；保存到工作组；工具栏「同步首页设置」支持复制/补全到组内用户
- 验收要点：授权 Tab 可见且可保存；未配置时「默认」勾选；同步后组内用户 `home_page_keys` 更新；用户重新登录/刷新 getInfo 后首页按授权显示
- 状态：已完成
- 完成说明：`sys_post.home_page_keys`；`syncHomePageToPostUsers`；前端授权 Tab + 同步按钮

### 2026-09-24 — 未授权首页回落改为「默认」并点亮勾选
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：系统管理 → 用户管理 → 授权 → 首页设置；登录后首页
- 现状：未配置时授权页「默认」未勾选，首页回落「完整」
- 期望：从未配置时授权页默认勾选「默认」并点亮；所有用户打开系统首页先进入「默认」视图
- 验收要点：未配置用户打开授权见「默认」已勾选；登录进首页为默认八卡布局；显式勾选完整/采购等仍可切换
- 状态：已完成
- 完成说明：`spd-ui` 授权回填 `['simple']`；`index.vue` `allowedHomeViews`/偏好回落改为 `simple`

### 2026-09-22 — 用户授权「首页设置」扩展：默认/完整/采购/库房/科室
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：系统管理 → 用户管理 → 授权 → 首页设置
- 现状：仅「简洁」「完整」；简洁命名易误解；缺角色向首页入口
- 期望：「简洁」改为「默认」；新增采购、库房、科室三项；后三项首页暂空，后续逐个完善
- 验收要点：授权勾选可保存；未勾选仍默认「默认」首页；勾选采购/库房/科室后可切换
- 状态：已完成
- 完成说明：前端授权选项与 `index.vue`；后端 `joinHomePageKeys`/`splitHomePageKeys` 放行新 key

### 2026-09-22 — 用户授权增加「首页设置」Tab
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：系统管理 → 用户管理 → 授权
- 现状：授权弹窗无首页视角控制；用户均可切换简洁/完整
- 期望：消息提醒后增加「首页设置」；可勾选简洁/完整；均未勾选时默认显示「默认」首页
- 验收要点：授权保存后对应用户刷新 getInfo；未授权仅见「默认」；勾选后可切换
- 状态：已完成
- 完成说明：`sys_user.home_page_keys`（column.sql / add_user_home_page_keys_public.sql）；用户授权 Tab；首页按 `homePageKeys` 过滤

### 2026-09-20 — 打印设置：移除更多检索并补齐操作按钮权限
- 类型：问题/缺陷
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：基础资料 → 打印设置
- 现状：检索区有「更多检索」；工具栏新增/修改/删除与操作列因缺 `system:printSetting:*` 按钮权限不显示
- 期望：普通检索字段；有页面权限的用户可见新增及行内修改/设默认/删除
- 验收要点：刷新前端后无更多检索；重新登录后工具栏与操作列按钮可见且可点
- 状态：已完成
- 完成说明：前端 `system/printSetting/index.vue`；脚本 `fix_print_setting_buttons_public.sql`（3961–3964）写入 `menu.sql`

### 2026-09-20 — 高值分栏分组标题「高值追溯」改为「追溯管理」
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：高值管理分栏 / 目录 3960
- 现状：二级目录与叶子页同名「高值追溯」，分组标题易混淆
- 期望：分组标题为「追溯管理」；组内仍为高值核销确认、高值追溯
- 验收要点：重新登录后底部组名为追溯管理，组内两项名称不变
- 状态：已完成
- 完成说明：`rename_gz_trace_dir_to_trace_mgmt_public.sql`；`reorg_gz_trace_dir_with_confirm_public.sql` / `menu.sql` 同步

### 2026-09-20 — 高值分栏再次出现两个「高值追溯」
- 类型：问题/缺陷
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：高值管理分栏 / 高值追溯
- 现状：空目录 3960 与页面 1238 同挂在 gz 一级，分栏假分组「高值管理」下出现两个「高值追溯」；核销确认仍在「高值使用」；`menu.sql` `ON DUPLICATE KEY UPDATE` 会把 1238/3850 父级改回旧挂法导致回归
- 期望：仅一组「高值追溯」目录，组内为高值核销确认 + 高值追溯页；种子脚本与现网一致
- 验收要点：重新登录后底部组名为高值追溯，组内两项、无重复叶子；高值使用下无核销确认
- 状态：已完成
- 完成说明：现网重跑 `reorg_gz_trace_dir_with_confirm_public.sql` + `remove_gz_goods_audit_menu_3858_public.sql`；`menu.sql` 23.9.2/23.11 改为目录 3960 下挂 3850/1238

### 2026-09-09 — 出库弹窗表头布局与审核后打印
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：库房管理 / 出库申请、出库审核
- 现状：总金额在制单人后；备注过长；无制单日期/审核人/审核日期；打印位置不在审核后
- 期望：科室后总金额；制单人后制单日期；领用人后审核人、审核日期；备注缩短；审核完成后显示打印
- 验收要点：添加/查看出库表头顺序正确；未审核无打印；已审核打印在审核按钮后
- 状态：已完成
- 完成说明：`outWarehouse/apply/index.vue`、`outWarehouse/audit/index.vue`

### 2026-09-09 — 出库弹窗明细标题后增加打印（已审核才显示）
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：库房管理 / 出库申请、出库审核
- 现状：查看出库弹窗「出库明细信息」后无打印入口
- 期望：标题后增加打印；仅已审核（billStatus=2）显示
- 验收要点：三院、枣强打开已审核出库查看弹窗可见打印；未审核不显示
- 状态：已完成
- 完成说明：`outWarehouse/apply/index.vue`、`outWarehouse/audit/index.vue` → `handleModalPrint`

### 2026-09-09 — 入库审核弹窗明细标题后增加打印
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：库房管理 / 入库审核
- 现状：查看入库弹窗「入库明细信息」后无打印入口，需回列表操作列打印
- 期望：标题后增加打印按钮；仅已审核（billStatus=2）可点
- 验收要点：衡水三院、枣强打开已审核入库查看弹窗可打印；未审核时按钮禁用
- 状态：已完成
- 完成说明：`inWarehouse/audit/index.vue` → `handleModalPrint`

### 2026-09-08 — 耗材对照整页样式定为公共（衡水三院 + 枣强）
- 类型：需求
- 影响范围：全部租户（明确含 `hengsui-third-001` / `zaoqiang-tcm-001`）
- 菜单/模块：基础资料 / 耗材对照（`foundation/materialCompare/index`）
- 现状：对照页 UI 已按列表规范改完，需明确无租户分叉页、衡水/枣强同一套样式与交互
- 期望：左供应商列表 + 右固定查询区/工具栏/明细表；无「更多检索」；供应商名称搜索；编码/名称/收费规格可排序；表头行高与悬停/勾选高亮对齐耗材产品维护；勾选与操作列 sticky 整行高亮；顶底 8px 留白；左右约 18.75% / 81.25%
- 验收要点：衡水三院与枣强登录同一路由同一组件；页面内无 `isHsThirdTenant` / `isZqTcmTenant` 样式分支；无 `*Hs` / `*Zq` 对照专属页
- 状态：已完成
- 完成说明：仅共享页 `spd-ui/src/views/foundation/materialCompare/index.vue`；业务差异（如 HIS 绑定写字段）仍走后端公共接口，不拆前端样式页

### 2026-09-08 — 耗材对照列表交互对齐（公共）
- 类型：需求
- 影响范围：全部租户（含枣强）
- 菜单/模块：基础资料 / 耗材对照（`foundation/materialCompare/index`）
- 现状：有「耗材明细/当前供应商」标题行；拼音简码检索；左右比例约 5:19；编码/名称/收费规格不可排序；明细横滑条过细
- 期望：去掉标题行；拼音简码改为供应商名称搜索；左收窄、右加宽；耗材编码/耗材名称/收费规格可升序降序；明细框横向滚动条加粗
- 验收要点：衡水/枣强同一共享页；供应商搜索按供应商名称模糊；三列服务端排序；横滑条高度约 12px
- 状态：已完成
- 完成说明：前端 `materialCompare/index.vue`；后端 `FdMaterial.supplierName` + `FdMaterialMapper` 条件

### 2026-09-08 — 耗材对照移除更多检索（固定公共查询区）
- 类型：需求
- 影响范围：全部租户
- 菜单/模块：基础资料 / 耗材对照（`foundation/materialCompare/index`）
- 现状：搜索区使用「更多检索」动态条件 + 保存默认，与列表页公共规范不一致
- 期望：移除更多检索与「保存为默认显示条件」；固定展示项目编码/名称/拼音简码/规格/收费项目ID/是否对照/是否收费 + 搜索重置
- 验收要点：耗材对照查询区无「更多检索」；条件常显；衡水/枣强同一共享页
- 状态：已完成
- 完成说明：`materialCompare/index.vue` 改为固定 `compare-query-row` 查询区

### 2026-09-08 — 用户管理改为公共页（含枣强）
- 类型：需求
- 影响范围：全部启用耗材客户（含 HS_003、ZQ_TCM）
- 菜单/模块：系统管理 / 用户管理（`system/user/index`）
- 现状：共享页 UI 已对齐列表规范，但客户菜单可能未对枣强开通；抽屉「机构单位」曾用系统参数下标，易串院
- 期望：菜单 `default_open_to_customer=1`，回填 `hc_customer_menu` / 相关 `sys_user_menu` / super 岗位菜单；机构单位取当前登录租户 `customerName`；枣强仍禁手工新增/新增导入，走 HIS 人员同步
- 验收要点：枣强（`zaoqiang-tcm-001`）开通客户后可见「用户管理」；机构单位显示本租户名称；衡水可新增，枣强隐藏新增并提示 HIS 同步
- 状态：已完成
- 完成说明：SQL `open_system_user_menu_to_customers.sql`；前端 `organizationUnitDisplay`；页面逻辑仍 `isZqTcmTenant` 分支。当前开发库 `aspt` 尚无枣强客户行时，待建租户后需再跑该 SQL

### 2026-08-31 — 耗材产品维护名称检索误命中无关品
- 类型：问题/缺陷
- 影响范围：全部租户
- 菜单/模块：基础资料 / 耗材产品维护
- 现状：搜索「稀释液」或拼音简码 `xsy` 会返回大量名称无关的产品（如一次性血样采集容器等）；`样本稀释液`(简码 YBXSY) 有命中却被 `一次性使用`(YCXSY 含 XSY) 刷屏，观感像搜不到
- 期望：中文按名称/编码/规格等模糊包含匹配；纯字母按拼音简码支持首/尾/包含；短简码排除「一次性使用/YCXSY」夹心误命中；简码检索按相关度+简码长度排序
- 验收要点：搜「稀释液」仅命中名称等含「稀释液」；搜 `xsy` 能靠前看到 `样本稀释液`(YBXSY)，且不再被大量「一次性使用」占满首页
- 状态：已完成（待联调验收：重启后端 + 前端强刷）
- 完成说明：Service 将纯字母 `name` 自动升为 `nameSearch`（兼容旧包只传 name=xsy）；Mapper `choose` 优先 `nameSearch` 并排除 YCXSY 夹心误伤 + 相关度排序；前端字母检索显式 `delete name`；Controller GET/POST 简码模式不套表头排序

### 2026-08-21 — 耗材名称模糊检索中文与拼音条数不一致
- 类型：问题/缺陷
- 影响范围：全部租户
- 菜单/模块：基础资料 / 耗材产品维护
- 现状：输入拼音简码如 `fgf` 与中文「发光法」条数不一致（例如 56 vs 51）
- 期望：中文关键字按名称匹配，并与其拼音简码 OR；纯字母只按 `referred_name` 简码匹配，不再与名称条件 AND 收窄
- 验收要点：`发光法` 与 `fgf` 结果集接近（简码含 FGF 或名称含发光法均可命中）；纯字母不再误走名称字段 AND
- 状态：已废止（被 2026-08-31 条目替代：中文 OR 拼音简码会导致「稀释液」误命中大量无关品）
- 完成说明：原 OR 方案已回退；见 2026-08-31 条目

### 2026-08-21 — 耗材产品维护改为公共菜单（含枣强）
- 类型：需求
- 影响范围：全部启用耗材客户（含 HS_003、ZQ_TCM）
- 菜单/模块：基础资料 / 耗材产品维护（`foundation/material/index`）
- 现状：页面为共享页且已有枣强分支，但客户菜单可能未对枣强开通，导致侧栏看不到
- 期望：菜单 `default_open_to_customer=1`，并回填 `hc_customer_menu` / 已有基础资料用户的 `sys_user_menu` / super 岗位菜单
- 验收要点：枣强切换组织后可见「耗材产品维护」；仍保持枣强禁手工新增、编辑限字段；衡水行为不变
- 状态：已完成
- 完成说明：`column.sql` 回填公共授权；页面逻辑仍按 `isZqTcmTenant` / `isHsThirdTenant` 分支，未把单院特例写成默认

### 2026-08-12 — 出/退库汇总表增加「计费」列
- 类型：需求
- 影响范围：全部租户
- 菜单/模块：库房管理 / 出/退库查询 / 出/退库汇总表
- 现状：汇总表在「储存方式」后无「计费」列；明细表已有该列
- 期望：在「储存方式」后增加「计费」列，显示「是/否」，取值耗材档案维护 `is_billing`（与明细表一致）；导出同步带上该列
- 验收要点：汇总列表与导出均在储存方式后展示计费；是=档案计费，否=不计费/其它；全租户生效（非单院分支）
- 状态：已完成
- 完成说明：汇总 SQL 回传 `materialIsBilling`；`secondOutQuery` 与导出 `exportCTKWarehouseSummaryListStyledXlsx` 已增加计费列

### 2026-08-22 — 页签切回后列表页顶部留白被挡
- 类型：问题/缺陷
- 影响范围：全部租户
- 菜单/模块：全局布局（页签切换 / keep-alive）；典型复现：基础资料 / 耗材产品维护 ↔ 到货验收
- 现状：从其他页签切回缓存列表页时，主内容区 scrollTop 残留，固定头下整页上移，搜索区顶部 8px 留白消失
- 期望：任意页签切回后与首次打开该页布局一致
- 验收要点：耗材产品维护 → 到货验收 → 页签切回，顶部留白与首次打开一致；筛选/勾选缓存仍保留；其他模块页签切换无回归
- 状态：已完成
- 完成说明：`spd-ui` 新增 `utils/reset-main-scroll.js`；`permission.js` `afterEach` 与 `AppMain` 路由切换/过渡完成后统一调度滚动复位

### 2026-08-22 — 到货验收 RK-添加明细 弹窗布局优化
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：库房管理 / 到货验收 / 修改入库 → RK-添加明细（嵌套耗材选择弹窗）
- 现状：搜索区偏高、耗材关键字输入框偏长；明细表列名「库存分类」与业务用语不一致；标题栏与父级「修改入库」高度不一致；明细表高度与底部分页留白不协调
- 期望：压缩查询区内边距与行距；耗材输入框略短；表头改为「库房分类」；RK-添加明细 标题栏与修改入库一致；明细表占满可用高度，翻页区自分隔线起仅保留适度底边留白（约 8px）
- 验收要点：衡水、枣强切换组织后打开到货验收 → 修改入库 → 添加明细，布局一致；分页正常显示且不被裁切；入库审核等非嵌套场景仍可用 SelectMaterialFilter
- 状态：已完成
- 完成说明：`spd-ui` 共享组件 `SelectMaterialFilter.vue`（查询区/表头/嵌套标题栏/表格高度与分页样式）；`inWarehouse/apply/index.vue` 嵌套层样式覆盖；维护脚本 `scripts/build-select-material-filter.js`
- 2026-08-23 补充：修改入库明细表高度回退为 calc(100vh-420px)（与 7f7134c/参考图一致），移除错误的动态测量逻辑

### 2026-08-23 — 到货验收 RK-添加明细 底部分明细框对齐主列表
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：库房管理 / 到货验收 / 修改入库 → RK-添加明细
- 期望：嵌套弹窗底部明细框 DOM 结构、样式与高度算法与到货验收主列表 `apply-table-panel` 完全一致（白卡片 + `apply-main-table` + `apply-pagination-wrap`）
- 验收要点：对比主列表与 RK-添加明细：表格行高、横向滚动条与分页间距一致；无底部大块留白；修改入库主弹窗明细仍无分页、高度公式不变
- 状态：已完成
- 完成说明：`SelectMaterialFilter.vue` nested 分支重构为 `apply-table-panel` 结构，`updateFilterTableHeight` 对齐 `updateMainTableHeight`；`inWarehouse/apply/index.vue` 移除旧 `material-filter-detail-box` 嵌套样式

### 2026-08-23 — 入库审核列表页 UI 对齐到货验收
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：库房管理 / 入库审核
- 期望：搜索区白卡片、工具栏、主表明细框、分页、表头/滚动条/列宽与到货验收主列表完全一致；移除耗材筛选；搜索/重置紧跟引用单号；审核按钮置于工具栏左侧
- 验收要点：对比到货验收与入库审核：搜索容器高度/边框/控件尺寸一致；底部分页始终可见；横向滚动条 12px；列顺序与间距一致
- 状态：已完成
- 完成说明：`inWarehouse/audit/index.vue` 复用 `list-query-panel`、`list-toolbar`、`apply-table-panel`、`apply-main-table`、`apply-pagination-wrap` 及动态 `mainTableHeight` 布局

### 2026-08-23 — 退货申请列表页 UI 对齐到货验收
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：库房管理 / 退货申请
- 期望：搜索区、工具栏、主表明细框、分页、表头/滚动条/列宽与到货验收一致；移除更多检索/耗材筛选；勾选行高亮
- 状态：已完成
- 完成说明：`inWarehouse/refundGoodsApply/index.vue` 复用到货验收列表布局与 `apply-main-table` 样式体系

### 2026-08-23 — 退货审核列表页 UI 对齐到货验收
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：库房管理 / 退货审核
- 期望：与到货验收主列表布局、样式、勾选高亮完全一致；移除更多检索/耗材筛选
- 状态：已完成
- 完成说明：`inWarehouse/refundGoodsAudit/index.vue` 复用到货验收列表 `apply-table-panel` 体系

### 2026-08-23 — 出库/退库四列表页 UI 对齐到货验收
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：库房管理 / 出库申请、出库审核、退库申请、退库审核
- 期望：搜索区白卡片两行布局、工具栏、主表明细框、分页常驻、表头 13px、横向滚动条 12px、列间距与到货验收一致；移除更多检索/耗材筛选；勾选行高亮
- 验收要点：对比到货验收与上述四页：搜索容器高度/边框/控件尺寸一致；底部分页无数据时也显示；复选框选中行蓝色高亮、取消后恢复
- 状态：已完成
- 完成说明：
  - `outWarehouse/apply/index.vue`（出库申请）
  - `outWarehouse/audit/index.vue`（出库审核）
  - `outWarehouse/refundDepotApply/index.vue`（退库申请）
  - `outWarehouse/refundDepotAudit/index.vue`（退库审核）
  - 均复用 `list-query-panel`、`list-toolbar`、`apply-table-panel`、`apply-main-table`、`apply-pagination-wrap` 及 `mainTableHeight` / `selectedRowMap` 勾选高亮逻辑
  - 主列表移除底部「合计」行（与到货验收一致）；表头行高 34px，去除 scoped 中 `height: 50px` 旧规则

### 2026-08-23 — 盘点申请列表页 UI 对齐到货验收
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：库房管理 / 盘点申请
- 期望：与到货验收主列表布局、样式、勾选高亮完全一致；移除更多检索
- 状态：已完成
- 完成说明：`stocktaking/profit/index.vue` 复用 `list-query-panel`、`apply-table-panel`、`apply-main-table`、`apply-pagination-wrap` 及 `mainTableHeight` / `selectedRowMap` 勾选高亮逻辑

### 2026-08-23 — 盈亏处理列表页 UI 对齐到货验收
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：库房管理 / 盈亏处理
- 期望：与到货验收主列表布局、样式、勾选高亮完全一致；移除更多检索
- 状态：已完成
- 完成说明：`warehouse/profitLoss/index.vue` 复用 `list-query-panel`、`apply-table-panel`、`apply-main-table`、`apply-pagination-wrap` 及 `mainTableHeight` / `selectedRowMap` 勾选高亮逻辑

### 2026-08-23 — 期初库存导入列表页 UI 对齐到货验收
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：库房管理 / 期初库存导入
- 期望：与到货验收主列表布局、样式、勾选高亮完全一致；移除更多检索
- 状态：已完成
- 完成说明：`warehouse/initialStockImport/index.vue` 复用 `list-query-panel`、`apply-table-panel`、`apply-main-table`、`apply-pagination-wrap` 及 `mainTableHeight` / `selectedRowMap` 勾选高亮逻辑

### 2026-08-23 — 盘点入库列表页 UI 对齐到货验收
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：库房管理 / 盘点入库
- 期望：与到货验收主列表布局、样式、勾选高亮完全一致；移除更多检索
- 状态：已完成
- 完成说明：`stocktaking/in/index.vue` 复用 `list-query-panel`、`apply-table-panel`、`apply-main-table`、`apply-pagination-wrap` 及 `mainTableHeight` / `selectedRowMap` 勾选高亮逻辑

### 2026-08-23 — 调拨申请列表页 UI 对齐到货验收
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：库房管理 / 调拨申请
- 期望：与到货验收主列表布局、样式、勾选高亮完全一致；移除更多检索
- 状态：已完成
- 完成说明：`warehouseTransfer/apply/index.vue` 复用 `list-query-panel`、`apply-table-panel`、`apply-main-table`、`apply-pagination-wrap` 及 `mainTableHeight` / `selectedRowMap` 勾选高亮逻辑；补全非 scoped 布局块 `warehouseTransfer-apply-page`（表格卡片、表头、滚动条、行高亮）

### 2026-08-23 — 调拨审核列表页 UI 对齐到货验收
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：库房管理 / 调拨审核
- 期望：与到货验收主列表布局、样式、勾选高亮完全一致；移除更多检索
- 状态：已完成
- 完成说明：`warehouseTransfer/audit/index.vue` 复用与到货验收/入库审核一致的 `list-query-panel`、`apply-table-panel`、`apply-main-table`、`apply-pagination-wrap` 及 `mainTableHeight` / `selectedRowMap` 勾选高亮；移除了 `more-search-bar` 与主表合计行

### 2026-08-23 — 科室申领列表页 UI 对齐到货验收
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：科室管理 / 科室申领
- 期望：与到货验收主列表布局、样式、勾选高亮完全一致；移除更多检索
- 状态：已完成
- 完成说明：`department/dApply/index.vue` 复用 `list-query-panel`、`apply-table-panel`、`apply-main-table`、`apply-pagination-wrap` 及 `mainTableHeight` / `selectedRowMap` 勾选高亮逻辑；搜索区改为两行固定字段（单号、科室、日期、单据状态）

### 2026-08-23 — 申领单审核列表页 UI 对齐到货验收
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：科室管理 / 申领单审核
- 期望：与到货验收主列表布局、样式、勾选高亮、表头排序完全一致；移除更多检索
- 状态：已完成
- 完成说明：`department/dApplyAudit/index.vue` 复用 `list-query-panel`、`apply-table-panel`、`apply-main-table`、`apply-pagination-wrap` 及 `mainTableHeight` / `selectedRowMap` 勾选高亮；搜索区两行（单号、仓库、科室、制单/审核日期切换、单据状态）；列表样式纳入 `department-apply-list-align.scss`（`d-apply-audit-page`）

### 2026-08-23 — 科室申购列表页 UI 对齐到货验收
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：科室管理 / 科室申购
- 期望：与到货验收主列表布局、样式、勾选高亮、表头排序完全一致；移除更多检索
- 状态：已完成
- 完成说明：`department/dPurchaseAgg/index.vue` 复用 `list-query-panel`、`apply-table-panel`、`apply-main-table`、`apply-pagination-wrap` 及 `mainTableHeight` / `selectedRowMap` 勾选高亮；搜索区两行（申购单号、仓库、科室、日期、单据状态）

### 2026-08-24 — 高值/结算/财务八列表页 UI 对齐到货验收（统一样式扩展）
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：高值订单 / 备货退货、备货退库、跟台管理；结算 / 结算申请、结算审核；财务 / 发票管理、仓库结算单、供应商结算单
- 期望：列表页（搜索区 + 工具栏 + 主表 + 分页）与到货验收主列表布局、样式一致；勾选行高亮；弹窗不动
- 状态：已完成
- 完成说明：上述 8 页 page-class 纳入 `department-apply-list-align.scss`；移除页内 `margin-top: -20px` 等失效列表 CSS；结算/财务页补齐 `apply-table-panel` / `mainTableHeight` / `apply-pagination-wrap`

### 2026-08-24 — 科室管理八列表页 UI 对齐到货验收（统一样式）
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：科室管理 / 科室申购、申购单审核、转科申请、科室审核、收货确认、科室批量消耗、盘点申请（科室）、申领单审核
- 期望：列表页（搜索区 + 工具栏 + 主表 + 分页）与到货验收主列表布局、样式完全一致；勾选行高亮；备注列不排序；弹窗不动
- 状态：已完成
- 完成说明：新增全局 `department-apply-list-align.scss`（8 页 page-class 统一，含 `d-apply-audit-page`）；各页移除失效旧 CSS 选择器；`receiptConfirm` 为样板页先行验收；`dApplyAudit` 清理 `</style>` 外残留列表 CSS，列表样式走全局 SCSS

### 2026-08-24 — 库房管理十列表页 UI 回归对齐（含到货验收）
- 类型：问题/缺陷 + 技术改造
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：库房管理 / 到货验收、退货申请、退货审核、退库审核、盘点申请、盈亏处理、期初库存导入、盘点入库、调拨申请、调拨审核
- 现状：全局 `department-apply-list-align.scss` 扩展后，到货验收等页仍保留页内重复列表 CSS（含 `margin-top: -20px`），与统一方案冲突，搜索区留白/滚动条/表头表现异常；部分页 CSS 选择器 class 与模板不一致（如 `refund-goods-audit-page`）；盘点申请仍用固定 `calc(100vh - 340px)` 高度
- 期望：上述 10 页列表区与科室管理已对齐页一致，列表样式完全走全局 SCSS；页内仅保留弹窗/打印等特例；弹窗打开时 `is-modal-open` 冻结主表布局
- 验收要点：对比收货确认/入库审核与上述各页：搜索白卡片两行、底部分页常驻、横向滚动条 12px、勾选行蓝色高亮；到货验收主列表不再出现搜索区被上拉或异常竖排；盘点申请主表高度随窗口自适应
- 状态：已完成
- 完成说明：`department-apply-list-align.scss` 纳入 10 个 page-class；`inWarehouse/apply/index.vue` 移除页内重复主列表 CSS，保留弹窗/RK-添加明细特例；其余 9 页删除 `margin-top: -20px` 及重复列表块，修正 class 选择器；`warehouse/stocktaking/index.vue` 补齐 `apply-table-panel` / `mainTableHeight` / `apply-pagination-wrap`

### 2026-08-24 — 盘点/新品/库房申请/结算等九列表页 UI 对齐
- 类型：需求 + 技术改造
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：库房管理 / 盘点申请、盈亏处理、期初库存导入、盘点入库；科室管理 / 新品申购申请、新品申购审批、科室请购、库房申请单；结算 / 结算申请
- 现状：盘点申请（`stocktaking/profit`）仍有 `margin-top: -20px` 与页内重复列表 CSS；新品申购申请/审批、库房申请单、结算申请仍用 `more-search-bar` 或固定 `54vh`/`calc` 表高，未纳入统一布局
- 期望：上述页面列表区与收货确认等已对齐页一致（两行固定搜索、白卡片明细框、分页常驻、`mainTableHeight`）；列表样式走全局 SCSS；弹窗不动
- 验收要点：对比收货确认与上述各页：搜索白卡片两行、底部分页常显、横向滚动条 12px、勾选行蓝色高亮（有勾选的页）；盘点申请不再出现搜索区上拉；新品申购/库房申请单表高随窗口自适应
- 状态：已完成
- 完成说明：`department-apply-list-align.scss` 新增 `stocktaking-profit-page` / `new-product-apply-page` / `new-product-audit-page` / `wh-warehouse-apply-page`；`stocktaking/profit` 清理冲突列表 CSS；新品申购申请/审批、库房申请单、结算申请改为固定两行搜索并补齐 `apply-table-panel`；盈亏处理、期初库存导入、盘点入库、科室请购（`dPurchaseAgg`）此前已对齐，本轮抽查通过

### 2026-08-24 — 消息提醒弹窗明细框加宽加高、横向滚动条加粗
- 类型：问题/缺陷
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：全局 / 消息提醒（`AppMain` 内弹窗）
- 现状：科室未收货确认等明细区受 `max-width: 576px` 限制，右侧大块留白；表高固定 256；表内横向滚动条仅 6px，左右拖动难操作
- 期望：明细框铺满右侧内容区；表高随视口自适应；横向滚动条高度 12px（对齐列表页）
- 验收要点：打开消息提醒 → 科室预警 → 科室未收货确认：表格占满右侧白区、出库单号不易被截断；横向滚动条可轻松拖动；仓库预警各 Tab 同样铺满
- 状态：已完成
- 完成说明：`layout/components/AppMain.vue` 去掉 `.wh-reminder-panel-inner` 576px 限宽；单据/近效期表改用视口自适应 `max-height`；明细表横向滚动条加粗至 12px

### 2026-08-24 — 消息提醒「科室库存预警」明细表
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：全局 / 消息提醒 → 科室预警 → 科室库存预警
- 现状：该 Tab 仅展示跳转提示文案，无明细数据
- 期望：明细框展示序号、科室名称、耗材编码/名称、规格、单位、单价、数量、下限数量、上限数量、生产厂家；口径与科室库存查询「科室库存预警」一致
- 验收要点：打开消息提醒 → 科室预警 → 科室库存预警：表格铺满右侧、列齐全；数量低于下限或高于上限的科室+耗材汇总行可见；双击耗材编码跳转科室库存预警 Tab
- 状态：已完成
- 完成说明：新增 `/dashboard/home/departmentReminderInventoryAlertList`；`AppMain.vue` 替换占位文案为明细表

### 2026-08-24 — 消息提醒「科室销提醒」Tab + 移除接口失败红字
- 类型：需求 + 问题/缺陷
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：全局 / 消息提醒 → 科室预警
- 现状：科室库存预警接口未部署时页面展示 `Request failed with status code 404`；无「科室销提醒」Tab
- 期望：接口失败时不展示 HTTP 错误文案；「科室有效期预警」后增加「科室销提醒」，明细含处理状态/开单科室/执行科室/病历号/姓名/收费编码/名称/规格/单位/单价
- 验收要点：后端未重启时科室库存预警 Tab 仅显示空表无红字；科室销提醒 Tab 展示待处理低值计费明细；Tab 角标显示条数
- 状态：已完成
- 完成说明：新增 `/dashboard/home/departmentReminderConsumeList`；`AppMain.vue` 增加 consume Tab 与明细表；科室预警各 Tab 加载失败静默

### 2026-08-24 — 科室销提醒对齐患者费用明细 + 科室权限 + 仅未处理
- 类型：问题/缺陷
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：消息提醒 → 科室销提醒；科室管理 → 患者收费查询
- 现状：科室销提醒与患者费用明细查询口径不一致；未严格按科室数据权限与「未处理」筛选
- 期望：按当前用户科室权限查患者费用明细；仅 `PENDING_CONSUME`（未处理）；已处理/已消耗等不展示；患者收费查询列表同步按科室权限过滤
- 验收要点：患者收费查询「未处理」条数与科室销提醒一致（同科室权限下）；已处理行不出现在科室销提醒
- 状态：已完成
- 完成说明：`HisPatientChargeServiceImpl` 科室销提醒复用 `fromAllQuery`+`processed=N`+低值口径；`selectUnifiedMirrorPageSlice` 统一加科室 scope

（在此下方追加）

### 2026-09-20 — 患者收费查询再次回到科室一级假分组
- 类型：问题/缺陷
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：科室管理 / 患者收费查询 → 科室消耗
- 现状：3601 父级又挂回科室一级，底部假「科室管理」下出现患者收费查询
- 期望：仍在「科室消耗」下，与扫码消耗等同组
- 验收要点：重新登录后仅在科室消耗组内可见；底部假分组无该项
- 状态：已完成
- 完成说明：重跑 `move_patient_charge_to_consume_public.sql`；`menu.sql` 23.3.1 强制 parent=1559 + 授权回填（`/` 分段，SqlInitRunner 更新重启自动执行）

### 2026-09-18 — 患者收费查询移至科室消耗下
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM；业务仍以衡水计费为主）
- 菜单/模块：科室管理 / 患者收费查询 → 科室消耗
- 现状：患者收费查询挂在科室一级假分组「科室管理」下
- 期望：挂到「科室消耗」下，与扫码消耗、批量消耗、消耗追溯同组
- 验收要点：重新登录后科室消耗组内可见患者收费查询；原底部假分组不再出现该项
- 状态：已完成
- 完成说明：`move_patient_charge_to_consume_public.sql`（3601 → parent 1559）

### 2026-09-18 — 数据中心菜单精简（公共）
- 类型：需求
- 影响范围：全部租户（含 HS_003 `hengsui-third-001`、ZQ_TCM `zaoqiang-tcm-001`）
- 菜单/模块：数据中心
- 现状：决策性报表设备类菜单过多；含医院医疗设备平台、BI设备大屏等
- 期望：移除医院医疗设备平台；决策性报表仅保留耗材领用收入同比、高耗单品使用统计、全院耗材领用同比；数据中心叶子保留 BI数据分析（耗材）、效能分析报表、数字孪生监控大屏；移除 BI数据分析（设备）
- 验收要点：衡水三院、枣强重新登录后数据中心分栏仅见上述保留项（全院类报表其它项仍在，不含医院医疗设备平台）
- 状态：已完成
- 完成说明：`trim_datacenter_menus_public.sql`

### 2026-09-18 — 高值分栏：底部「高值管理」改为「高值追溯」，迁入高值核销确认
- 类型：需求
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：高值管理分栏
- 现状：叶子菜单被假分组标题成「高值管理」；「高值核销确认」挂在「高值使用」下
- 期望：分组标题为「高值追溯」；「高值核销确认」与「高值追溯」同组
- 验收要点：重新登录后底部组名为高值追溯，组内含高值核销确认、高值追溯；高值使用下不再有核销确认
- 状态：已完成
- 完成说明：`reorg_gz_trace_dir_with_confirm_public.sql`（目录 3960 ← 3850/1238）

### 2026-09-18 — 移除高值重复菜单「退库审核」
- 类型：技术改造
- 影响范围：全部租户（明确含 HS_003 `hengsui-third-001`、ZQ_TCM `zaoqiang-tcm-001`）
- 菜单/模块：高值管理 / 退库审核（`gzOrder/goodsAudit`，menu_id=3858）
- 现状：与备货管理下「备货退库」（1197，`gzOrder/refund` + `/gz/refundStock`，类型 301）功能重复
- 期望：主菜单及两院授权中移除「退库审核」及按钮；统一使用「备货退库」
- 验收要点：衡水三院、枣强重新登录后高值分栏无「退库审核」；备货退库可正常新增/审核
- 状态：已完成
- 完成说明：脚本 `remove_gz_goods_audit_menu_3858_public.sql`；静态路由去掉 `goodsAudit`；`menu.sql` 23.6.4 改为清理

### 2026-08-24 — 患者费用明细：执行科室检索 + 科室权限 OR + 列顺序
- 类型：问题/缺陷
- 影响范围：全部租户（含 HS_003、ZQ_TCM）
- 菜单/模块：科室管理 → 患者收费查询 → 患者费用明细
- 现状：按执行科室搜索无效；数据权限仅「优先执行科室」回退开单科室，开单/执行分属不同授权科室时误过滤；退费关联 ID 列位置不合理
- 期望：执行科室编码/名称/简码可检索；账号对开单科室或执行科室任一有权限即展示；退费关联 ID 列移至处理时间之后
- 验收要点：住院/门诊 Tab 输入执行科室条件后列表条数变化；仅有开单科室权限的记录可见、仅有执行科室权限的亦可见、两者皆无则不可见；列顺序符合期望
- 状态：已完成
- 完成说明：`unifiedDeptScopeHisIdMatch` 改为开单 OR 执行；`execDeptNameLike` 规范化；前端默认展示执行科室检索；退费关联 ID 列后移并去掉重复「处理方」列；二次加固见下条

### 2026-08-24 — 患者费用明细科室权限二次加固
- 类型：问题/缺陷
- 影响范围：全部租户
- 现状：非机构管理员仍可能看到无权限科室计费行
- 期望：仅展示开单或执行科室在 sys_user_department 授权内的记录
- 完成说明：Service 注入 scopeDeptIds；SQL 增加 his_id/code/id 三向对照；无科室授权 denyAll；导出同步 scope
- 状态：已完成（三次：仅 fd_department.code 对照；1056 内一科 code=609 不应见 479）
